DATABASE_URL="file:./prisma/dev.db"
PORT=3000
# 默认 0.0.0.0（便于局域网访问）；若你的环境限制 0.0.0.0 监听，可改成 127.0.0.1
HOST=0.0.0.0
XDG_CACHE_HOME="./.cache"

# 开发默认账号（seed 用）
DEFAULT_ADMIN_USERNAME="admin"
DEFAULT_ADMIN_PASSWORD="admin123456"
DEFAULT_ADMIN_DISPLAY_NAME="管理员"
